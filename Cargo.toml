[package]
name = "tmux-picker"
version = "1.0.0"
edition = "2024"
description = "TUI session picker for tmux on SSH login"

[dependencies]
ratatui = "0.30"
crossterm = "0.29"
clap = { version = "4", features = ["derive"] }
